services:
  postgres:
    image: postgres:17-alpine
    environment:
      POSTGRES_USER: taxinator
      POSTGRES_PASSWORD: taxinator
      POSTGRES_DB: taxinator
    ports:
      - "5435:5432"
    volumes:
      - /Users/seth/Documents/el-taxinator-data/pgdata:/var/lib/postgresql/data
    restart: unless-stopped
